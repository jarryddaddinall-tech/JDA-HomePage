/**
 * Site-wide defaults for launch. Set `siteUrl` to your real domain (https, no trailing slash).
 * Optional: add og-image.jpg (1200×630) to this folder and set ogImage path.
 * Used by head snippets; update meta tags on each HTML page to match, or find-replace the domain once.
 */
window.SITE = {
  siteUrl: "https://jarrydaddinall.com",
  siteName: "Jarryd Addinall",
  defaultDescription: "Sound, apps, and writing in one place.",
  /** Path from site root, or full URL to your social preview image */
  ogImage: "/og-image.jpg",
};
